#summary List of features
#labels Featured
----
= Features by Role =
|| || *probeuser* || *poweruser* || *poweruserplus* || *manager* ||
|| *Contexts* || list, status, list attributes, view servlets/mappings, view filters/mappings, request count, session count || start, stop, remove attributes, view web.xml || || deploy, undeploy, view context.xml ||
|| *Sessions* || list, status, list attributes, last-accessed IP, search || remove attributes, expire || || ||
|| *JSPs* || || list, compile, view JSP source, view servlet source || || pre-compile on deployment, discard ||
|| *Data Sources* || list, group by JDBC URL, status (max/established/busy connections) || reset || test, view query history, execute SQL || ||
|| 
|| *Logs* || list, tail in real-time, download || || || ||
|| *Threads* || list || view execution stack || || kill ||
|| *Connectors* || status, real-time usage charts || || || ||
|| *Cluster* || status, real-time traffic charts || || || ||
|| *JVM* || status, real-time memory usage charts || || || advise GC ||
|| *System* || || || || properties, OS details ||
|| *Java Service Wrapper* || status || || || restart JVM ||
|| *Quick Check* || || || || view ||

= Compatibility =
  * *Java:* 1.4, 1.5, 6
  * *Tomcat:* 5.0, 5.5, 6.0 (partial)
  * *JBoss:* 3.2, 4.0
  * *Data Sources:* DBCP, C3P0, Oracle
  * *Logs:* JDK (java.util), log4j, commons-logging